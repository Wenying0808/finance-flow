import { Dayjs } from 'dayjs';
export interface Expense {
    id: string; //generated by uuid when adding expense
    date: string | Dayjs;
    category: string;
    description: string;
    amount: number;
}